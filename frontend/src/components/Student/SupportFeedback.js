// frontend/src/components/Student/SupportFeedback.js
import React from 'react';

const SupportFeedback = () => {
    return (
        <div>
            <h4>Support and Feedback</h4>
            <p>This is where students can submit support requests or provide feedback to the university.</p>
            {/* Add your UI elements here */}
        </div>
    );
};

export default SupportFeedback;
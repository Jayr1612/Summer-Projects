// frontend/src/components/Student/CourseRegistration.js
import React from 'react';

const CourseRegistration = () => {
    return (
        <div>
            <h4>Register for Courses</h4>
            <p>This is where students can browse available courses and register for them.</p>
            {/* Add your UI elements here */}
        </div>
    );
};

export default CourseRegistration;